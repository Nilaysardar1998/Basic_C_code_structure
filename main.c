#include <stdio.h>

int main() {
    // Here (unsigned) is modifier, (int) is data type, (c) is variable and identifier, and 10 is assigned value to c.
    unsigned  int c = 10;

    printf("C contain value of %d", c);
    return 0;
}
